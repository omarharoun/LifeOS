{
  "name": "railway",
  "version": "0.0.0",
  "private": true,
  "description": "A personal launcher — a surface only when you need one. (MVP, milestone M0)",
  "main": "electron/main.js",
  "scripts": {
    "build": "node build.mjs",
    "watch": "node build.mjs --watch",
    "start": "npm run build && electron .",
    "test": "npm run build && node test/generate.test.js && node test/route.test.js && node test/gmail.test.js",
    "gmail-auth": "node electron/gmail-auth.js",
    "selftest": "npm run build && electron electron/selftest.js"
  },
  "devDependencies": {
    "electron": "^33.2.1",
    "esbuild": "^0.24.2"
  },
  "dependencies": {
    "@anthropic-ai/sdk": "^0.65.0",
    "googleapis": "^144.0.0",
    "react": "^18.3.1",
    "react-dom": "^18.3.1",
    "zod": "^4.4.3"
  }
}
